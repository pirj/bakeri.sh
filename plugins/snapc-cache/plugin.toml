description = "Inspect snapcompose's layered snapshot cache: per-plugin entries, sizes, kinds"
protocol_version = "1"
deps = []
host_deps = []
# Command-only plugin; framework dispatch_command falls back to all
# discoverable plugins for command lookup.
triggers = []
commands = ["snapc-cache"]

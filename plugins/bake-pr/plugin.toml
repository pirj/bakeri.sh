description = "Run a GitHub PR (including from untrusted forks) in the project's VM"
protocol_version = "1"
deps = []
host_deps = ["gh", "git"]
# Command-only plugin; framework dispatch_command falls back to all
# discoverable plugins for command lookup.
triggers = []
commands = ["bake-pr"]
